# Current live data

Google Apps Script should eventually write normalized current-election JSON files here.

These files are allowed to update frequently and should be clearly marked as `live_unverified`, `draft`, or `final` in metadata.

The starter app currently loads `2024-president-ga-draft.json` from this folder.
